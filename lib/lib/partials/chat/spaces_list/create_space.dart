import 'package:flutter/material.dart';

class CreateSpace extends StatelessWidget {
  const CreateSpace({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
